# Backend API base URL
# In development, the Vite proxy handles /api/* and /verifyme/* — this variable
# is used only for QR code generation (the URL embedded in the QR payload).
VITE_API_URL=http://localhost:3000
